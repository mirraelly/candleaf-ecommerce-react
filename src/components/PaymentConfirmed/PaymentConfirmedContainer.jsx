import styled from "styled-components";

const PaymentConfirmedContainer = styled.section`
  .confirmed-cicle-icon {
    color: rgba(86, 178, 128, 60%);
    font-size: 6.25em;
  }

  .title-payment-confirmed {
    font-family: "Poppins", sans-serif;
    font-weight: 500;
    font-size: 1.625em;
    line-height: 1.5;
    letter-spacing: -0.9px;
  }

  .subtitutle-payment-confirmed,
  .text-start {
    font-family: "Helvetica", "Arial", sans-serif;
    font-weight: 400;
    text-indent: 1rem;
    font-size: 0.875em;
    line-height: 0.975em;
    letter-spacing: 0;
  }
`;

export default PaymentConfirmedContainer;
